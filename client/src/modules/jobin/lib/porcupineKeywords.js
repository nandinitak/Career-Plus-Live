const porcupineKeywords = [
  {
    label: "कौशल", // Example label (remove "_wasm.ppn" and replace "_" with spaces)
    sensitivity: 0.9, // Adjust sensitivity as needed
    publicPath: "keywords/कौशल_hi_wasm_v3_0_0.ppn", // Public path relative to the 'public' folder
    customWritePath: "1.0.0_wake_word_hi_wasm.ppn", // Versioned path (adjust as needed)
  },
];

export default porcupineKeywords;
